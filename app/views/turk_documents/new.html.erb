<%= react_component("TurkDocumentForm",
                    props: {
                      docUrl: @doc_url,
                      docId: @doc_id,
                      pageCount: @page_count,
                      existingFields: @labels,
                    },
                    prerender: false) %>
  
